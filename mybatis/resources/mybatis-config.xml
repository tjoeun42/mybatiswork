<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
  <!-- setting : mybatis 구동시 선언할 설정들을 작성하는 영역 -->
  <!-- 만약 null로 데이터가 전달되었다면 빈칸이 아닌 null로 인식하도록 설정 -->
  <settings>
  	<setting name="jdbcTypeForNull" value="NULL"/>
  </settings>
  
  <!-- typeAlias : servlet, bean클래스들의 풀클래스명을 단순한 클래스명으로 하기 위해 별칭을 등록해주는 영역 -->

  <!-- mybatis에서 연동할 DB정보들을 등록하는 영역 -->	
  <environments default="development">
    <environment id="development">
      <!--
      	* transactionManager type
      	  JDBC : 트랜잭션을 내가 직접 관리함(수동 commit)
      	  MANAGED : 트랜잭션에 어떠한 영향도 행사하지 않겠다(자동 commit)
       -->
      <transactionManager type="JDBC"/>
      <!--
      	* dataSource type
      	  POOLED : ConnectionPool을 사용함
      	  UNPOOLED : ConnectionPool을 사용하지 않음
       -->
      <dataSource type="POOLED">
        <property name="driver" value="oracle.jdbc.OracleDriver"/>
        <property name="url" value="jdbc:oracle:thin:@localhost:1521:free"/>
        <property name="username" value="mybatis"/>
        <property name="password" value="1234"/>
      </dataSource>
    </environment>
  </environments>
  <!-- mapper : 실행할 sql문들을 기록 해둔 mapper파일들을 등록하는 영역 -->
  <mappers>
    <mapper resource="org/mybatis/example/BlogMapper.xml"/>
  </mappers>
</configuration>